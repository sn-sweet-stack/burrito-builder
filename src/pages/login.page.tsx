import SignInButton from '../components/Auth/signInButton.component';

const LoginPage = () => {
  return (
    <section>
      <div>
        <SignInButton />
      </div>
    </section>
  );
};

export default LoginPage;
